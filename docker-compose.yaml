version: "3"
services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./facebook-link-tree:/app/facebook-link-tree
    environment:
      - PYTHONPATH=/app
    command: ["uvicorn", "facebookspy.src.server.backend.app:app", "--host", "0.0.0.0", "--port", "8000"]
